[project]
name = "mydev-toy"
version = "0.1.0"
description = "Toy PrivateUse1 backend (CMake + C++), loaded via torch.ops.load_library"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.uv]
dev-dependencies = [
  "debugpy>=1.8.0",
]



